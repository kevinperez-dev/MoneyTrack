{
  "name": "moneytrack",
  "version": "1.0.0",
  "description": "Proyecto MoneyTrack con frontend React, backend Express y PostgreSQL",
  "private": true,
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c blue,green \"npm --prefix money-track-backend run dev\" \"npm --prefix money-track-frontend run dev\"",
    "backend": "npm --prefix money-track-backend run dev",
    "frontend": "npm --prefix money-track-frontend run dev",
    "backend:start": "npm --prefix money-track-backend start",
    "frontend:build": "npm --prefix money-track-frontend run build",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "3.6.2"
  }
}